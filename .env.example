# production으로 설정 시 CORS_ORIGIN에 명시된 도메인만 허용 (미설정 시 모든 origin 허용)
NODE_ENV=
PORT=3000
AUTH_PASSWORD=                  # 선택 — 비우면 첫 접속 시 웹 UI 셋업 화면이 뜬다.
                                #        값이 있으면 1회만 사용 후 hash로 변환·저장됨.
SESSION_SECRET=
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
TUNNEL_ENABLED=false
TUNNEL_NAME=
LOG_RETENTION_DAYS=30

# 진단 로그 (콘솔 + logs/aikombinat.log). 미설정 시 info.
AIKOMBINAT_LOG_LEVEL=           # debug | info | warn | error
AIKOMBINAT_LOG_DIR=             # 선택 — 기본값: <app data>/logs 또는 <repo>/logs
